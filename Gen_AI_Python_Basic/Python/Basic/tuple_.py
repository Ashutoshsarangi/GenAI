#Tuple

single_tuple = (1, )
int_not_tuple = (1) # not a tuple, integer

# Tuple with multiple data types
mixed_tuple = ("Ashu", 30)

#destructureing tuple
name, age = mixed_tuple

#concatenating tuple
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
concatenated_tuple = tuple1 + tuple2

print(f'repeating tuple: {tuple2 * 3=}')

